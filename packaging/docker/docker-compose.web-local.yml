services:
  mysql:
    image: mysql:8.4
    container_name: openwork-web-local-mysql
    ports:
      - "3306:3306"
    environment:
      MYSQL_ROOT_PASSWORD: password
      MYSQL_DATABASE: openwork_den
    healthcheck:
      test: ["CMD-SHELL", "mysqladmin ping -h 127.0.0.1 -ppassword --silent"]
      interval: 5s
      timeout: 5s
      retries: 30
      start_period: 10s
    volumes:
      - openwork-web-local-mysql-data:/var/lib/mysql

  redis:
    image: redis:7.4-alpine
    container_name: openwork-web-local-redis
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 30
      start_period: 5s

volumes:
  openwork-web-local-mysql-data:
