apiVersion: v1
kind: Service
metadata:
  name: {{ include "openwork-ee.denWorkerProxyServiceName" . }}
  labels:
    {{- include "openwork-ee.componentLabels" (dict "root" . "component" "den-worker-proxy") | nindent 4 }}
spec:
  type: {{ .Values.denWorkerProxy.service.type }}
  ports:
    - name: http
      port: {{ .Values.denWorkerProxy.service.port }}
      targetPort: http
  selector:
    {{- include "openwork-ee.componentSelectorLabels" (dict "root" . "component" "den-worker-proxy") | nindent 4 }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "openwork-ee.fullname" . }}-den-worker-proxy
  labels:
    {{- include "openwork-ee.componentLabels" (dict "root" . "component" "den-worker-proxy") | nindent 4 }}
spec:
  replicas: {{ .Values.denWorkerProxy.replicaCount }}
  selector:
    matchLabels:
      {{- include "openwork-ee.componentSelectorLabels" (dict "root" . "component" "den-worker-proxy") | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "openwork-ee.componentSelectorLabels" (dict "root" . "component" "den-worker-proxy") | nindent 8 }}
        {{- with .Values.denWorkerProxy.podLabels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      {{- with .Values.denWorkerProxy.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: den-worker-proxy
          image: "{{ .Values.denWorkerProxy.image.repository }}:{{ default .Values.image.tag .Values.denWorkerProxy.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.denWorkerProxy.containerPort }}
          envFrom:
            - configMapRef:
                name: {{ include "openwork-ee.configName" . }}
            - secretRef:
                name: {{ include "openwork-ee.secretName" . }}
          env:
            - name: PORT
              value: {{ .Values.denWorkerProxy.containerPort | quote }}
            {{- range $key, $value := .Values.denWorkerProxy.env }}
            - name: {{ $key }}
              value: {{ $value | quote }}
            {{- end }}
          livenessProbe:
            httpGet:
              path: /health
              port: http
            {{- toYaml .Values.denWorkerProxy.probes.liveness | nindent 12 }}
          readinessProbe:
            httpGet:
              path: /ready
              port: http
            {{- toYaml .Values.denWorkerProxy.probes.readiness | nindent 12 }}
          {{- with .Values.denWorkerProxy.resources }}
          resources:
            {{- toYaml . | nindent 12 }}
          {{- end }}
